{
  "name": "smartmetric-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "nuxi cleanup",
    "dev": "nuxt dev",
    "dev:fast": "nuxt dev --no-fork",
    "dev:host": "nuxt dev --host 0.0.0.0 --port 3001",
    "dev:clean": "nuxi cleanup && nuxt dev",
    "build:clean": "nuxi cleanup && nuxt build",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "start": "node .output/server/index.mjs"
  },
  "dependencies": {
    "ant-design-vue": "^4.2.6",
    "axios": "^1.7.7",
    "echarts": "^6.0.0",
    "nuxt": "^3.12.4",
    "vue-echarts": "^8.0.1"
  }
}
